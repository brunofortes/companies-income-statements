package main

import (
	"fmt"
)

func main() {
	fmt.Println("Demonstração de Resultados das empresas listadas na Bolsa de Valores.")

	/*companyConnection := NewCompanyConnection()
	companies, error := companyConnection.ListAll()

	if error != nil {
		fmt.Println(error)
		log.Fatal(error)
	}

	fmt.Println(companies)*/
}
